// Update these values for your repository.
window.BLOG_CONFIG = {
  // GitHub owner (username or organization)
  repoOwner: "daredumidu",
  // GitHub repo name where posts/ folder lives
  repoName: "your-repo-name",
  // branch or commit sha (optional). If omitted, GitHub API will use the default branch.
  branch: "main"
};

// Example:
// window.BLOG_CONFIG = { repoOwner: "your-username", repoName: "blog-repo", branch: "main" };